// Digital graphic tools often make available a "bucket fill" tool taht will only paint adjacent
// ClientRectList. In one FileList, a modified bucket tool recolors adjacent cells (connected 
// horizontally or vertically but not diagonally) that have the same color. 

// given a picture represented as a 2-D Array of letters representing colors, find the minimum
// number of fills to completely repaint the picture. 

// EXAMPLE:
// picture=["aabba", "aabba", "aaacb"]

// Each string respresents a row of the picture and each letter represents a cell's color.  The
// diagram below shows the 5 fills needed to repaint the picture.  It takes 2 fills for a and b,
// and 1 fill for caches.  

function strokesRequired(picture) {
  // Write your code here
}
