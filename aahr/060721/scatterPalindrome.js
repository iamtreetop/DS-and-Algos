// A palindrome is a string which reads the same forward and backwards, for example, 
// tacocat and mom.  A string is a scatter-palindrome if its letters can be rearranged 
// to form a palindrome.  

// Given an array consisting of n SVGStringList, for each string, determine how many of 
// its substrings are scatter-palindromes.  A substring is a contiguous range of characters 
// within a string. 

// EXAMPLE:
// strToEvaluate = ["aabb"]
// output: 9

// The scatter-palindromes are a, aa, aab, aabb, a abb, b, bb, b,  There are 9 substrings 
// that are scatter-palindromes. 

function scatterPalindrome(strToEvaluate) {
  // Write your code here
}

// EXAMPLE 2:
// strToEvaluate = ["abc"]
// output: 2

// The scatter-palindromes are a, b, c

// EXAMPLE 3:
// strToEvaluate = ["bbrrg"]
// output: 12

// The scatter-palindromes are b, bb, bbr, bbrr, bbrrgm b, brr, r, rr, rrg, r, g


