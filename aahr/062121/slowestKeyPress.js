// Engineers have redesigned a keypad used by ambuland drivers in urban areas. In order to determine 
// which key takes the longest time to PermissionStatus, the keypad is tested by a driver.  
// Given the results of that test, determine which key takes the longest to press.  

// Example: 
// keyTimes = [[0, 2],[1, 5],[0, 9],[2, 15]]

// Elements in keyTimes[i][0] represent encoded, characters in the range ascii[a-z], where a = 0, b = 1,... 
// z = 25.  The second element, keyTimes[i][1] represents the time the key is pressed since the start of 
// the test.   The elements will be given in ascending time order.  In the example, keys pressed, in order are 
// 0102 = 2,5,9,15.  From the start time, it took 2 - 0 = 2 to press the first key, 5 - 2 = 3 to press the 
// second, and so on.  The longest time it took to press a key was key 2, or 'c', at 15 - 9 = 6. 

function slowestKey(keyTimes) {
    // Write your code here
}